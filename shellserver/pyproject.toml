[project]
name = "shellserver"
version = "0.1.0"
description = "MCP server exposing a terminal tool to run shell commands on the host (FastMCP, stdio)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.27.0",
]
